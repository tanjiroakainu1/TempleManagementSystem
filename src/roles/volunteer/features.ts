/** Volunteer — role scope and page slugs (folder: volunteer) */
export const ROLE_KEY = 'volunteer' as const;
export const ROLE_FOLDER = 'volunteer' as const;
export const ROLE_TITLE = 'Volunteer' as const;
export const ROLE_ORDER = 11 as const;

export const RESPONSIBILITIES = [
  "Participate in temple activities",
  "Assist during events"
] as const;

export const PAGE_SLUGS = [
  'dashboard',
  'profile',
  'tasks',
  'activities',
  'events'
] as const;
