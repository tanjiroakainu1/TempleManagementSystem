import { getRoleProfile } from '@/config/roleProfiles';
import { ROLE_KEY } from './features';

/** visitor — role profile metadata (role #14) */
export const PROFILE = getRoleProfile(ROLE_KEY);
export default PROFILE;
